1、当前线程所执行的字节码的行号指示器，每个线程独有一份,取好后给执行引擎使用，获取当前执行指令，然后执行
2、占用空间小，线程私有，与线程生命周期一致，pcregister没有OOM没有GC，其余的有溢出风险
3、为什么使用PC寄存器来使用，线程切换后能恢复到正确的执行位置，每个线程都需要独有program counter register
4、简单说明一下
    并行 VS 串行 同时通过 VS 依次通过
    并发，CPU在不同的线程中来回切换，主频率很高，看着像是并行，其实是并发