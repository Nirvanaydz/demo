redis的事务：https://www.bilibili.com/video/BV1oW411u75R?p=20
multi
watch|unwatch
exec|discard

事务中一个执行报一个错error，全体连坐
事务中执行没报错，但是逻辑上有错误，只有这一个不执行，冤头债主


watch类似于乐观锁，被监视的key在事务执行前（未exec）被其他的session修改了，那么事务执行报nli。
乐观锁（常用）和悲观锁：提交的版本必须大于当前的版本才可以更新
乐观锁：别人用完了我不知道，傻傻的认为我拿到的就是最新的，当执行失败的时候，会取出最新的值进行操作，然后提交，若失败，继续区最新的操作然后修改直至成功
悲观锁：只有我用完了才可以使用